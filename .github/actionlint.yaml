# Targeted actionlint exceptions; VALIDATE_GITHUB_ACTIONS is enabled in CI.
# Shellcheck SC2015 (A && B || C) in workflow scripts is a known pattern; suppress.
ignore:
  - 'shellcheck reported issue in this script: SC2015:.+'
