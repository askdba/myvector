# PR #76 – Status Review (2026-03-07)

**Checkpoint tag:** `pr76-checkpoint-2026-03-07`  
**Branch:** `feature/component-migration-8.4-9.6`  
**PR:** https://github.com/askdba/myvector/pull/76

---

## Executive Summary

PR #76 has been in progress for weeks. Despite substantial work on component migration, CI/release workflows, and documentation, **CI still fails on 4 jobs**. The PR cannot merge until CI is green. Meanwhile, **reviewer-requested code and doc changes (Phase 1–2 of PR76_PROJECT.md) remain largely unaddressed**, creating a "endless state" where CI fixes and review feedback are pursued in parallel without closure.

---

## What Has Been Done

### Completed

| Area | Status | Notes |
|------|--------|------|
| **Component architecture** | Done | `myvector_component.cc`, binlog service, UDF service, config, query rewrite |
| **Component builds (8.0, 8.4, 9.0)** | ✓ | Out-of-tree build with MYSQL_SOURCE_DIR; CI passes |
| **Component tests (8.0, 8.4, 9.0)** | ✓ | Install → UDFs → uninstall → reinstall; CI passes |
| **Plugin builds (8.0, 8.4, 9.0)** | ✓ | In-tree plugin build; CI passes |
| **Lint** | ✓ | cppcheck passes |
| **BUILD_MODES.md** | Done | Documents plugin vs component parity and differences |
| **PR76_PROJECT.md** | Done | Checklist of reviewer items |
| **PR76_REVIEW_RESPONSE.md** | Done | Maps review feedback to actions |

### In Progress / Blocked

| Area | Status | Blocker |
|------|--------|--------|
| **build-component-9-6** | Fails | Docker build fails at "Installing build dependencies" in oraclelinux:9 (dnf install) |
| **test (plugin 8.0, 8.4, 9.0)** | Fails | `ERROR 1125: Function 'myvector' already exists` during reinstall step |
| **Reviewer items (Phase 1–2)** | 0/6 done | fsync, charset metadata, stub comments, failure-mode docs, etc. |

---

## Why We Are Still at This Stage

### 1. CI Failures Block Merge

Two distinct CI failure modes:

**A. build-component-9-6**  
- Script: `scripts/build-component-9.6-docker.sh`  
- Uses `oraclelinux:9` + libs extracted from `mysql:9.6.0`  
- Fails during `dnf install -y ...` (exit code 1)  
- Likely cause: dnf/mirror timeout, missing package, or oraclelinux:9 environment change  
- Impact: MySQL 9.6 component build not validated in CI

**B. test (plugin)**  
- All three plugin test jobs (8.0, 8.4, 9.0) fail in the **reinstall** step  
- Sequence: DROP FUNCTIONs → UNINSTALL PLUGIN → INSTALL PLUGIN → CREATE FUNCTIONs  
- Error: `Function 'myvector' already exists` (occurs on INSTALL PLUGIN or first CREATE FUNCTION)  
- CI only drops 4 UDFs; `sql/myvectorplugin.sql` registers 7 (including `myvector_search_open_udf`, `myvector_row_distance`, `myvector_ann_set`)  
- Possible causes:  
  - Plugin or MySQL registers a function named `myvector` that is not dropped  
  - Incomplete DROP list in CI (missing UDFs)  
  - MySQL version-specific behavior on UNINSTALL PLUGIN / UDF cleanup  

### 2. Reviewer Feedback Not Addressed

Per `docs/PR76_REVIEW_RESPONSE.md` and `docs/PR76_PROJECT.md`:

| # | Item | Priority | Status |
|---|------|----------|--------|
| 1.1 | fsync before rename in `persist_binlog_state()` | High | Not done |
| 1.2 | Charset metadata for component UDFs | Medium | Not done |
| 1.3 | Stub comments in build headers | Low | Not done |
| 2.1 | Failure-mode docs for binlog state | High | Not done |
| 2.2 | BUILD_MODES.md expansion | Partial | Done |
| 3.1 | CI manifest/plugin path logging | Optional | Not done |

The PR cannot be approved until these are addressed. The focus has been on CI (build-component-9-6, plugin tests) rather than on reviewer items.

### 3. Parallel Workstreams, No Clear Order

- **Stream A:** Fix CI (build-component-9-6, plugin reinstall)  
- **Stream B:** Implement reviewer feedback (fsync, charset, docs)  

Both are needed for merge. There is no single "next step" that unblocks everything. The PR feels endless because:

1. CI fails → fix CI → new failure or regression  
2. Reviewer items pile up → addressing them doesn’t fix CI  
3. No checkpoint or "merge-ready subset" defined  

---

## Recommended Path Forward

### Option A: Fix CI First (Unblock Green Build)

1. **Plugin test:** Align CI with `sql/myvectorplugin.sql`:  
   - Add `DROP FUNCTION IF EXISTS myvector_search_open_udf`, `myvector_row_distance`, `myvector_ann_set` before reinstall  
   - If error persists, add `DROP FUNCTION IF EXISTS myvector` (if such a function exists)  
   - Or temporarily **skip the plugin reinstall test** and file an issue to fix it later  

2. **build-component-9-6:**  
   - Inspect full dnf error output in CI logs  
   - Consider retries, different mirror, or splitting dnf install into smaller steps  
   - Or mark 9.6 as optional / allow failure until MySQL 9.6 is stable  

### Option B: Address Reviewer Feedback First

1. Implement high-priority items: fsync (1.1), failure-mode docs (2.1)  
2. Implement medium/low: charset metadata (1.2), stub comments (1.3)  
3. Push and run CI; fix any regressions  

### Option C: Narrow Scope for Merge

1. **Temporarily disable** failing jobs (`build-component-9-6`, plugin `test`) so CI is green  
2. Merge with known gaps; track fixes in follow-up issues  
3. Complete reviewer items in this PR or a quick follow-up  

---

## Checkpoint Commands

```bash
# Restore this state
git checkout pr76-checkpoint-2026-03-07

# Or create a branch from it
git checkout -b pr76-resume-$(date +%Y%m%d) pr76-checkpoint-2026-03-07
```

---

## Files Touched in Recent Work

- `.github/workflows/ci.yml`, `.github/workflows/release.yml`
- `scripts/build-component-9.6-docker.sh`, `scripts/sandbox-component-test.sh`
- `include/hnswdisk.i`, `include/mysql_component_service_base.h`
- `src/component_src/*.cc`, `src/myvector.cc`, `src/myvector_plugin.cc`
- `docs/BUILD_MODES.md`, `docs/PR76_PROJECT.md`, `docs/PR76_REVIEW_RESPONSE.md`
