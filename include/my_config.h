/**
 * Stub for standalone component build when MYSQL_BUILD_DIR is not set.
 * MySQL server generates this during its build; minimal stub so includes succeed.
 */
#ifndef MY_CONFIG_INCLUDED
#define MY_CONFIG_INCLUDED

/* Leave empty; component build only needs the include to resolve. */

#endif /* MY_CONFIG_INCLUDED */
